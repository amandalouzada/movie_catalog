import { YoutubeImgPipe } from './youtube-img.pipe';

describe('YoutubeImgPipe', () => {
  it('create an instance', () => {
    const pipe = new YoutubeImgPipe();
    expect(pipe).toBeTruthy();
  });
});
